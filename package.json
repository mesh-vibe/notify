{
  "name": "meshvibe-notify",
  "version": "0.1.0",
  "description": "Unified notification routing across macOS, push, SMS, and email",
  "type": "module",
  "bin": {
    "notify": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "node_modules/.bin/tsc -p tsconfig.build.json",
    "dev": "node_modules/.bin/tsc -p tsconfig.build.json --watch",
    "test": "node_modules/.bin/vitest run",
    "test:watch": "node_modules/.bin/vitest",
    "lint": "node_modules/.bin/tsc --noEmit"
  },
  "keywords": [
    "meshvibe",
    "notification",
    "cli"
  ],
  "author": "MeshVibe",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
